import { icon, safe, state } from './app.js';
import { heading, eyebrow, link, sampleNote } from './pages.js';
import { websiteLogo } from './website-logo.js';

function website() {
  return `<div class="page example-page">${sampleNote('A product website, built with the shared Slang foundations.')}<div class="marketing"><nav class="marketing-nav" aria-label="Product"><a class="product-brand" href="#/website" aria-label="Slang home">${websiteLogo()}</a><div><a href="#/library">Explore</a><a href="#/docs">Learn</a><a href="https://github.com/Bitspark/slang">GitHub ↗</a>${link('#/studio', 'Try Slang', 'sd-button--accent sd-button--small')}</div></nav><section class="marketing-hero"><div>${eyebrow('IDEAS, IN FLOW')}<h1>Think in connections.<br/>Build with <em>Slang.</em></h1><p>Turn the way you think into the things you make. Connect small, reusable operators and watch your ideas come to life.</p><div class="hero-actions">${link('#/studio', 'Start creating', 'sd-button--accent')}${link('#/docs', 'See how it works', 'sd-button--outline')}</div><div class="marketing-proof"><span>Open source</span><i></i><span>Visual by nature</span><i></i><span>Built to be understood</span></div></div><div class="marketing-graph"><div class="window-bar"><i></i><i></i><i></i><span>city-population.slang</span></div><slang-flow variant="overview"></slang-flow><div class="graph-run-line"><span class="sd-badge sd-badge--success"><i class="sd-dot"></i>Flow complete</span><code>3 cities · 1 useful answer</code></div></div></section><section class="marketing-principles"><div><span>01</span><h2>See the whole idea.</h2><p>Understand what happens to your data by following its path.</p></div><div><span>02</span><h2>Build on small things.</h2><p>Make something useful once. Connect it to something new.</p></div><div><span>03</span><h2>Keep experimenting.</h2><p>Change an input, run the flow, and learn from the result.</p></div></section><section class="marketing-cta"><div><span class="section-index">YOUR FIRST CONNECTION</span><h2>Every big idea starts small.</h2><p>Try the interactive “Double a number” example.</p></div>${link('#/studio', 'Open the studio', 'sd-button--accent')}</section><footer class="marketing-footer"><span class="marketing-attribution"><a href="#/website" aria-label="Slang home">${websiteLogo()}</a><span>by Bitspark</span></span><div><a href="#/heritage">Our design</a><a href="#/docs">Documentation</a><a href="https://slang.run/">Live studio ↗</a></div></footer></div></div>`;
}

function definition() {
  return `name: Double a number
input: number
operators:
  multiply:
    factor: ${state.factor}
connections:
  input: multiply.in
  multiply.out: output
output: number`;
}
function inspector() {
  const d = {
    input: ['Input', '#', 'The starting point. A number supplied by the person running this flow.'],
    transform: [
      'Multiply',
      '×',
      'A small arithmetic operator. It multiplies the incoming number by a configurable factor.',
    ],
    output: ['Output', '↗', 'The result of your connected idea, ready to be used elsewhere.'],
  }[state.node];
  return `<span class="inspector-glyph">${d[1]}</span><h2>${d[0]}</h2><span class="sd-badge">${state.node === 'transform' ? 'Elementary operator' : 'Main service'}</span><p>${d[2]}</p><div class="inspector-ports"><span>Type</span><slang-type kind="number"></slang-type></div>`;
}
function studio() {
  return `<div class="page studio-page">${sampleNote('Interactive design example · calculations run locally in your browser.')}<div class="studio-app"><header class="studio-toolbar"><div class="studio-title"><span class="mini-mark">${icon('flow')}</span><div><strong>Double a number</strong><span>My blueprints / getting started</span></div></div><div class="studio-actions"><span class="saved-label">${icon('check')}Ready</span><button id="save-example" class="sd-button sd-button--outline sd-button--small">Save example</button><button id="run-flow" class="sd-button sd-button--accent sd-button--small">${icon('play')}Run flow</button></div></header><div class="studio-body"><aside class="studio-library"><div class="pane-title">Operators <span>3</span></div><p class="pane-description">The pieces in this flow</p>${[
    ['input', '#', 'Input', 'number'],
    ['transform', '×', 'Multiply', 'number'],
    ['output', '↗', 'Output', 'number'],
  ]
    .map(
      ([id, glyph, title, type]) =>
        `<button class="studio-op ${id === state.node ? 'selected' : ''}" data-node-select="${id}"><span>${glyph}</span><div>${title}<small>${type}</small></div></button>`,
    )
    .join(
      '',
    )}<div class="studio-library-bottom"><span class="section-index">WHAT TO TRY</span><p>Change the input or factor, then run the flow. Select an operator to inspect it.</p><a href="#/library">Explore the library ↗</a></div></aside><section class="studio-canvas" aria-label="Visual programming canvas"><div class="canvas-toolbar"><div class="sd-tabs" role="group" aria-label="Canvas view"><button data-studio-view="graph" aria-pressed="true">Graph</button><button data-studio-view="definition" aria-pressed="false">Definition</button></div><span>number → number</span></div><div id="graph-view"><slang-flow id="studio-flow" selected="${state.node}" result="${state.result}"></slang-flow><div class="canvas-type-legend"><slang-type kind="number"></slang-type><span>Each connection carries a number</span></div></div><div id="definition-view" hidden><pre class="sd-code">${definition()}</pre><p>This readable design example is illustrative, not an importable Slang blueprint.</p></div></section><aside class="inspector"><div class="pane-title">Inspector ${icon('layers')}</div><div id="node-details">${inspector()}</div><hr class="sd-divider"/><label class="sd-label" for="flow-input">Input value</label><input class="sd-input" type="number" id="flow-input" step="any" value="${state.input}"/><span class="sd-field-help">The number entering your flow.</span><label class="sd-label factor-label" for="flow-factor">Multiply by</label><input class="sd-input" type="number" id="flow-factor" step="any" value="${state.factor}"/><span class="sd-field-help">A configurable operator property.</span><div class="inspector-note">${icon('book')}<a href="#/docs">Understand operators and ports</a></div></aside></div><section class="console"><header><span>${icon('terminal')}Output console</span><span class="console-meta">Local example</span></header><div class="console-body"><div><span class="console-label">INPUT</span><code id="console-input">${state.input}</code></div><span class="console-arrow">→</span><div><span class="console-label">OUTPUT</span><output id="console-output" aria-live="polite">${state.result}</output></div><div class="console-message" id="console-message" role="status">Ready to run. Change a value and follow the flow.</div></div></section></div></div>`;
}

function docs() {
  return `<div class="page docs-page">${heading('EXAMPLE / DOCUMENTATION', 'Understand the flow.', 'A quiet place for concepts, references, and your next small discovery.')}<div class="docs-layout"><nav class="docs-nav" aria-label="On this page"><span class="nav-label">GETTING STARTED</span><a href="#concepts" data-scroll="concepts" class="active">Thinking in operators</a><a href="#ports" data-scroll="ports">Inputs & outputs</a><a href="#connections" data-scroll="connections">Making connections</a><a href="#first-flow" data-scroll="first-flow">Your first flow</a><hr class="sd-divider"/><a href="#/library">Operator reference ${icon('arrow')}</a></nav><article class="docs-article"><div class="article-meta"><span class="sd-badge">Concepts</span><span>4 minute read</span></div><h2 id="concepts">Small operators.<br/>Larger possibilities.</h2><p class="article-lead">A Slang program is a set of connected operators. Each one does a little work. Together, they describe how an idea becomes a result.</p><p>You can think of an operator as a box with a purpose. Some perform a calculation. Others transform a piece of data or decide which path it should follow.</p><div class="doc-figure"><slang-flow></slang-flow><span>A number enters, gets multiplied, and leaves as a result.</span></div><h3 id="ports">Inputs and outputs</h3><p>Ports are the places where data enters or leaves an operator. A port has a type, so the shape of the data stays clear as you build.</p><table class="sd-table"><thead><tr><th>Type</th><th>Meaning</th><th>Example</th></tr></thead><tbody><tr><td><slang-type kind="number"></slang-type></td><td>A numeric value</td><td><code>21</code></td></tr><tr><td><slang-type kind="string"></slang-type></td><td>A piece of text</td><td><code>"Hello, Slang"</code></td></tr><tr><td><slang-type kind="boolean"></slang-type></td><td>True or false</td><td><code>true</code></td></tr></tbody></table><h3 id="connections">Making connections</h3><p>A connection carries data from an output to an input. Start with compatible types, then follow the path through your program. The line itself is a part of the explanation.</p><div class="sd-callout"><strong>Keep the meaning visible.</strong><br/>Color helps you recognize types, but the labels tell the whole story. Never rely on color alone.</div><h3 id="first-flow">Your first flow</h3><ol class="steps"><li><span>1</span><div><strong>Start with a number</strong><p>Give the input a value, like 21.</p></div></li><li><span>2</span><div><strong>Connect a multiplication</strong><p>Set its factor to 2.</p></div></li><li><span>3</span><div><strong>Follow the result</strong><p>Run the flow. Your output is 42.</p></div></li></ol>${link('#/studio', 'Try this example', 'sd-button--accent')}<hr class="sd-divider"/><p class="fine-print">This page demonstrates the documentation design. For the runtime's full reference, see <a href="https://github.com/Bitspark/slang">Slang on GitHub</a>.</p></article><aside class="docs-note"><div class="small-grid-mark">${icon('flow')}</div><span class="section-index">A WAY OF THINKING</span><p>Make the structure visible.<br/>Let the details follow.</p><a href="#/library">Find an operator ↗</a></aside></div></div>`;
}
function deploymentRows() {
  return state.deployments
    .map(
      (d, i) =>
        `<tr><td><div class="deployment-name"><span class="deployment-icon">${icon('flow')}</span><span><strong>${safe(d.name)}</strong><small>${safe(d.description)}</small></span></div></td><td><span class="sd-badge sd-badge--${d.status === 'Running' ? 'success' : 'warning'}"><i class="sd-dot"></i>${d.status}</span></td><td>${safe(d.runtime)}</td><td class="numeric">${Number(d.runs).toLocaleString()}</td><td><button class="sd-button sd-button--quiet sd-button--small" data-deploy-toggle="${i}" aria-label="${d.status === 'Running' ? 'Pause' : 'Start'} ${safe(d.name)}">${icon(d.status === 'Running' ? 'pause' : 'play')}${d.status === 'Running' ? 'Pause' : 'Start'}</button></td></tr>`,
    )
    .join('');
}
function cloud() {
  const running = state.deployments.filter((d) => d.status === 'Running').length;
  return `<div class="page">${sampleNote('A fictional workspace. Changes stay in this browser; no services are deployed.')}<div class="workspace-banner"><div class="workspace-avatar">S</div><div><span>PERSONAL WORKSPACE</span><strong>Studio experiments</strong></div><span class="sd-badge">Demo workspace</span></div>${heading('EXAMPLE / SLANG CLOUD', 'A home for your connected ideas.', 'Keep your blueprints and running flows in one clear view.', `<button class="sd-button sd-button--accent" id="new-deployment">${icon('plus')}New deployment</button>`)}<div class="metrics"><div><span>Deployments</span><strong>${state.deployments.length}<small>in this workspace</small></strong><i>${icon('layers')}</i></div><div><span>Running flows</span><strong>${running}<small>ready to receive data</small></strong><i class="metric-success">${icon('play')}</i></div><div><span>Example executions</span><strong>${state.deployments.reduce((sum, d) => sum + Number(d.runs), 0).toLocaleString()}<small>illustrative sample data</small></strong><i>${icon('flow')}</i></div></div><section class="deployment-panel"><header><h2>Your deployments</h2><span class="sd-badge">${state.deployments.length} total</span></header><div class="table-scroll"><table class="sd-table"><thead><tr><th>Name</th><th>Status</th><th>Runtime</th><th>Runs</th><th><span class="sr-only">Actions</span></th></tr></thead><tbody>${deploymentRows()}</tbody></table></div>${state.deployments.length ? '' : '<div class="empty-state"><h2>A fresh workspace</h2><p>Create your first example deployment to see it here.</p></div>'}</section><div class="cloud-bottom"><section><span class="section-index">NEXT SMALL DISCOVERY</span><h2>Give an idea somewhere to start.</h2><p>Browse the operator library and discover a building block for your next flow.</p>${link('#/library', 'Explore the library', 'sd-button--outline')}</section><section class="workspace-note"><span class="mini-mark">${icon('book')}</span><h3>Learn by connecting</h3><p>Our first-flow guide explains operators, ports, and the connections between them.</p><a href="#/docs">Read the guide ${icon('arrow')}</a></section></div></div>`;
}
export const examplePages = { website, studio, docs, cloud, inspector, definition };
